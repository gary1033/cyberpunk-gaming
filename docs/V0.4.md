# V0.4

## 本版內容

1. 凱的第二段記憶增加維護時的動作，並開放趙明／蕭博士兩條可暫離的追問。核對原件後才記已回答；兩份回答都取得後，可保存具名說明或待查問題。趙明製作舉報包時回應選擇。沒有新增救援或結局門檻。
2. 阿傑被施壓後收回指認紙；幽靈受出賣後的有限補救保留地址／證詞界線；先救人或先取證會立即改變蕭博士對質的開場。
3. 對話中「紀錄／閱讀」及遊戲選單提供對話回看、案件摘要、字級／速度設定。僅記已讀頁與已選選項，最多保存最近 300 頁／項；跟隨存檔，舊檔從本次開始記錄。案件摘要使用現有條件篩選，不列未開放地點。閱讀設定跨次啟動保存，下一段套用。
4. B 照護與 C 甦醒各一張完整場景 CG，沿用人物參考、手繪線條、冷暖照明。不透明 1672×941 PNG，原始生成／source／runtime 與提示詞均保存。B 增加照護日常與兩種條款反應；C 增加重新詢問名字的片刻。既有三主結局與分支保留。
5. 採用 Kenney Interface Sounds 七個 CC0 原音檔，以降低 14 dB 的基準播放，補四個舊音效與三個操作提示。對話時獨立音樂混音匯流排降低 5 dB，結束還原，不改使用者音量。休整同時補滿鷹眼，無倒數、無章節推進；閱讀期間不扣鷹眼能量。

## 來源與檔案

- 音效來源：https://kenney.nl/assets/interface-sounds 。原授權 `assets/audio/sfx/KENNEY-LICENSE.txt`，逐檔對應與 SHA256 見同目錄 manifest.json。
- CG：`assets/sprites/cg/cg_ending_b_care.png`、`cg_ending_c_awakening.png`；來源位於 `assets/generated/cg/`，原圖位於 `assets/generated/cg/raw/`。
- 新閱讀介面：`scripts/ui/case_notebook.gd`，沿用 SaveManager 保存閱讀設定、GameManager 保存已讀紀錄。

## 驗證狀態

- 新增 `tests/test_v04_experience.gd`：記憶追問取消／完成、責任選項、未讀內容不外洩、回看無副作用、磁碟存讀檔、舊檔、300 頁限制、字級／速度、音效解碼、音樂降音與休整通過。
- 素材稽核：156 項，0 錯誤、0 音效缺口；32 張歷史圖片仍缺單獨原圖追溯，不屬本版新增。
- 完整套件首輪 956 通過、1 失敗（舊測試誤把正式音效視為佔位檔）；修正該測試後 BGM 23 項全過。七條通關、18 組自由路線、分頁與調查回歸通過。GPU 新功能回歸通過且無錯誤，已檢視 1280×720／854×480 閱讀設定、摘要及結局 CG。 Windows V0.4 已匯出，主選單啟動檢查通過；不是完整封裝版通關驗證。
- 未進行真人朗讀、實體手機驗收。既有姐弟精確年表與跨章資料改派仍是後續工作，不因本批宣稱完成。

- 封裝 PCK 檢查通過版本、主選單及新 CG／音效／閱讀腳本載入（RELEASE_V04_PASS）；結束時仍有既有 ObjectDB／2 resources 釋放警告，尚未解決。封裝版完整真人通關未驗證。
## 整合紀錄

- 2026-09-22：將尚未提交的 V0.2～V0.4 更新依素材、程式／回歸、文件分段提交，從 `Codex/cyberpunk-game-ideas-Mok3L` 整合至遠端預設主分支 `claude/cyberpunk-game-ideas-Mok3L`。
- V0.2 與早期分頁掃描約 450 MB 的可重建 PNG 保留本機並排除 Git；測試程式、文字結果及 V0.4 驗收圖納入版本管理。遊戲 runtime、generated-source、原始圖、音效及授權完整保留。
- 推送前完整測試：957 通過、0 失敗、0 警告（`docs/verification/V0.4/prepush.log`）。遠端 CI 與合併結果以 GitHub PR 為準。
